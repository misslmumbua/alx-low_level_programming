#include "main.h"
/**
 * int _abs(int); computes the absolute value of an integer
 * @n computes the integer
 * Return: Always 0 (Success)
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
