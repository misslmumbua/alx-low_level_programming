#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always(Success)
 */
int main(void)
{
	char y;
        for (y = 'a'; y <= 'z'; y++)
	{
		putchar(y);
	}
	putchar('\n');
        return (0);
}     
